[tool:pytest]
DJANGO_SETTINGS_MODULE = backend.settings
asyncio_mode = auto

addopts =
    --strict-markers
    --disable-warnings
    --tb=short
    -v

env =
    DEBUG=True
    POSTGRES_HOST=localhost
    REDIS_HOST=localhost
